#include "../../lib/catch.hpp"
#include "../../src/simulation/planet.hpp"
#include "../../src/simulation/surface.hpp"

TEST_CASE("")
{
    REQUIRE(true);
    //REQUIRE_THROWS_AS(assert true == false, std::);
}

TEST_CASE("")
{
    REQUIRE(true);
    //REQUIRE_THROWS_AS(IO::getStartupConfigs(argc, argv), std::invalid_argument);
}
